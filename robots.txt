# เกมเพลิน (GamePlearn) — gameplearn.com
# V.1.6.5 (Audit) — ไฟล์นี้ต้องมีอยู่จริง: เว็บนี้ตั้ง not_found_handling แบบ
# single-page-application ไว้ ถ้าไม่มีไฟล์นี้ /robots.txt จะถูกตอบด้วย index.html
# ซึ่งทำให้ search engine และเครื่องมือภายนอกอ่านเว็บผิดทั้งเว็บ
#
# หน้าเว็บทั้งหมดเป็นหน้าสาธารณะที่ตั้งใจให้ค้นเจอ (หน้า Admin มีด่านล็อกอินของตัวเอง
# — robots.txt ไม่ใช่เครื่องมือกันคนเข้า และการใส่ Disallow จะกลายเป็นป้ายชี้ทางแทน)

User-agent: *
Allow: /
